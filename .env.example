# Optional fallback GitHub token for local development only.
# Leave empty if you prefer sending X-GitHub-Token in each request.
GITHUB_TOKEN=
# Optional port override if you want to run on a different port.
APP_PORT=8000
